{
  "name": "newsfellow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply newsfellow --local",
    "db:migrate:remote": "wrangler d1 migrations apply newsfellow --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "@types/node": "^24.0.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.129.1"
  }
}
